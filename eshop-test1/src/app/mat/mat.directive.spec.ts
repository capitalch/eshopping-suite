import { MatDirective } from './mat.directive';

describe('MatDirective', () => {
  it('should create an instance', () => {
    const directive = new MatDirective();
    expect(directive).toBeTruthy();
  });
});
